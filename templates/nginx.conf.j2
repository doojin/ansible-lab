upstream app {
    {% for host in groups['app'] %}
    server {{ host }};
    {% endfor %}
}

server {
    listen       80;
    server_name  localhost;

    location / {
        proxy_pass http://app;
    }
}